name: dumont-secrets

services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: dumont_secrets
      POSTGRES_USER: dumont_secrets
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5434:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dumont_secrets"]
      interval: 10s
      timeout: 5s
      retries: 5

  oidcwarden:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      # Database
      DATABASE_URL: postgresql://dumont_secrets:${DB_PASSWORD}@postgres:5432/dumont_secrets
      # Admin panel
      ADMIN_TOKEN: ${ADMIN_TOKEN}
      # Domain
      DOMAIN: https://secret.getdumont.ai
      # SSO — DISABLED until ZITADEL app is configured
      # To enable: set SSO_ENABLED=true, SSO_ONLY=true and fill SSO_CLIENT_ID + SSO_CLIENT_SECRET in .env
      SSO_ENABLED: "false"
      SSO_ONLY: "false"
      SSO_AUTHORITY: https://auth.getdumont.ai
      SSO_CLIENT_ID: ${SSO_CLIENT_ID:-placeholder}
      SSO_CLIENT_SECRET: ${SSO_CLIENT_SECRET:-placeholder}
      SSO_PKCE: "true"
      SSO_SCOPES: "email profile offline_access"
      SSO_AUTHORIZATION_CODE_FLOW: "true"
      # ZITADEL: project ID must be added as trusted audience
      SSO_AUDIENCE_TRUSTED: "^369674090853695492$"
      # Email (via Resend SMTP bridge)
      SMTP_HOST: smtp.resend.com
      SMTP_PORT: "587"
      SMTP_SECURITY: starttls
      SMTP_USERNAME: resend
      SMTP_PASSWORD: ${RESEND_API_KEY}
      SMTP_FROM: noreply@getdumont.ai
      SMTP_FROM_NAME: Dumont Secrets
      # Application settings
      SIGNUPS_ALLOWED: "false"
      INVITATIONS_ALLOWED: "false"
      PASSWORD_HINTS_ALLOWED: "false"
      SHOW_PASSWORD_HINT: "false"
      LOG_LEVEL: info
      # Rocket web server
      ROCKET_PORT: "8080"
      ROCKET_WORKERS: "4"
    ports:
      - "127.0.0.1:8092:8080"
    volumes:
      - data:/data

volumes:
  postgres-data:
  data:
